export default interface FormDto {
  email: string;
  lastName: string;
  firstName: string;
  infoSource: string;
  svcInterest: string;
  assocType: string;
  explanation: string;
  errorMsg: string;
}
